# W1 - API Session 01

## Link Drive ảnh kết quả test:
["W1\api_session_01\test_results"](https://drive.google.com/drive/folders/1W6HPJ54i76C1_MWbs6y3kBSVmT7VXmPz?usp=sharing)

## Yêu cầu hệ thống:
- Python >= 3.10

## Thiết lập và chạy:

1. Kích hoạt môi trường ảo:
   ```bash
   # Windows: 
   python -m venv .venv
   .venv\Scripts\activate
   # Linux/Mac: 
   python3 -m venv .venv
   source .venv/bin/activate
    ```
2. Cài đặt thư viện (flask):
   ```bash
   pip install -r requirements.txt
   # Kiểm tra
   flask --version
   ```

3. Khởi động file python theo bài tương ứng:
   ```bash
   python <Bài số x>.py
   ```

4. Mở terminal khác và test với `curl` (theo ví dụ trong các ảnh kết quả)
